# xrayJunitExtensionFlakyTestIssue

To see the difference between reports build by surefire vs EnhancedLegacyXmlReportGeneratingListener execute tests with:

`mvn test -Dsurefire.rerunFailingTestsCount=2
`
